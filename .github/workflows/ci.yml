name: CI

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    container: ghcr.io/<YOUR_ORG>/beidou-ci:latest
    steps:
      - uses: actions/checkout@v3
      - name: Normal Tests
        run: make check
